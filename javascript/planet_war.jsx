const React = require("react");
const ReactDom = require("react-dom");


document.addEventListener("DOMContentLoaded", () => {
  const root = document.getElementById('root');
  ReactDom.render(<span>Success!</span>, root);
});
